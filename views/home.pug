extends layouts/main
include mixins/videoBlock

block content  
    .videos
       each tomato in videos
        +videoBlock({
            title:tomato.title,
            views:tomato.views,
            videoFile:tomato.videoFile
        })