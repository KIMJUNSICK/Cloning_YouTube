extends layouts/main

block content  
    .user-profile
        .user-profile__header
            h4=user.avatarUrl
            h4.profile__username=user.name     
        if user.id === loggedUser.id
            .user-profile__btns 
                a(href=`/users${routes.editProfile}`)
                    button 💗 Edit Profile
                a(herf=`/users${routes.changePassword}`)
                    button 💕 Change Passoword